// Test stub for the `server-only` guard package, which throws when imported
// outside a React Server Component bundle. Aliased in vitest.config.ts so that
// server modules can be unit-tested in the jsdom environment.
export {}
